<style>
  .field_with_errors { display: inline; }
</style>

<%= form_for([@subcategory]) do |f| %>
  <% if @subcategory.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@subcategory.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @subcategory.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <p>
    <%= f.label :nazwa %><br>
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.label :kategoria %><br>
    <!-- <%= f.text_field :category_id %> -->
    <%= f.select(:category_id, options_from_collection_for_select(@categories, 'id', 'name',@subcategory.category_id)) %>
  </p>





  <!-- <% @categories.each do |c| %>
    <tr>
      <td><%= c.id %></td>
      <td><%= c.name %></td>
    </tr>
  <% end %> -->
  <p>
    <%= f.submit :dodaj %>
  </p>

  <!-- ddendums:
  <ul>
    <%= f.fields_for :tag do |t| %>
      <li>
        <%= t.text_field :tag_name %>


      </li>
    <% end %>
  </ul> -->
<% end %>
